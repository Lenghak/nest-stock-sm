// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  userId    String   @id @unique @default(uuid())
  firstName String   @db.VarChar(256)
  lastName  String   @db.VarChar(256)
  username  String   @db.VarChar(256)
  phone     String   @db.VarChar(256)
  email     String   @unique @db.VarChar(256)
  img       String   @db.Text()
  password  String   @db.VarChar(256)
  Address   Address?
  createdAt DateTime @default(now())
  updatedAt DateTime @default(now())
}

model Address {
  addressId String   @id @unique @default(uuid())
  userId    String   @unique
  user      User     @relation(fields: [userId], references: [userId])
  street    String   @db.VarChar(256)
  city      String   @db.VarChar(256)
  province  String   @db.VarChar(256)
  country   String   @db.VarChar(256)
  zipCode   Int      @db.Integer()
  createdAt DateTime @default(now())
  updatedAt DateTime @default(now())
}

model Products {
  productId          String  @id @unique @default(uuid())
  productCode        String  @db.VarChar(256)
  productName        String  @db.VarChar(256)
  productDescription String  @db.VarChar(2048)
  productType        String  @db.VarChar(256)
  productPrice       Decimal @db.Money()
  productCategory    String  @db.VarChar(256)
  barCode            String  @db.VarChar(256)
}
